# Canonical snapshot navigation

This repository-only note keeps the README cross-reference in the unchanged
pinned specification navigable. Read the [repository README](../../../README.md)
for adoption and the existing disposable validators, or the
[experimental-skill documentation](../../README.md) for this delivery.

This note is not an additional authoring instruction and is excluded from the
frozen eleven-file `.skill` distribution. The pinned specification, schema,
template and licence remain byte-for-byte unchanged. Their documentation links
are references, not permission to retrieve outside evidence or runtime dependencies.
